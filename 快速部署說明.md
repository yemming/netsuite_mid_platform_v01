# 🚀 快速部署 Edge Function

## ✅ 已完成
- ✅ Secrets 已設定

## 📋 現在要做的：重新部署程式碼

### 方法 1：使用 Dashboard（推薦，5 分鐘內完成）

1. **打開** https://supabase.com/dashboard
2. **選擇專案** `mjjhopllbogcxqsofjjw`
3. **點擊左側** "Edge Functions"
4. **點擊** `sync-netsuite` 函數
5. **點擊右上角** "Edit" 或直接點擊程式碼區域
6. **打開本地檔案**：`supabase/functions/sync-netsuite/index.ts`
7. **全選複製**（Cmd+A 然後 Cmd+C，共 620 行）
8. **在 Dashboard 中貼上**（刪除舊內容後 Cmd+V）
9. **點擊** "Save" 或 "Deploy"
10. **等待部署完成**

完成！🎉

---

### 方法 2：使用 CLI（如果需要命令行）

```bash
# 安裝 CLI
brew install supabase/tap/supabase

# 登入
supabase login

# 連結專案
cd /Users/mingyou/Documents/cursor/NetSuite_Platform
supabase link --project-ref mjjhopllbogcxqsofjjw

# 部署
supabase functions deploy sync-netsuite
```

---

## ✅ 部署後驗證

1. **觸發同步測試**
   - 在網站上進入「訂閱資料集」
   - 點擊任意資料集的同步按鈕

2. **查看日誌**
   - Dashboard → Edge Functions → sync-netsuite → Logs
   - 應該看到：
     - ✅ `環境變數檢查: { hasNetsuiteAccountId: true, ... }`
     - ✅ `初始化 NetSuite 客戶端，Account ID: TD3...`
   - 如果看到 `hasNetsuiteAccountId: false`，表示 Secrets 還沒正確設定

---

## ❓ 如果 Dashboard 沒有編輯功能

某些 Supabase 版本可能沒有直接編輯功能。這種情況：

1. 使用 CLI（方法 2）
2. 或告訴我，我可以幫你想其他辦法


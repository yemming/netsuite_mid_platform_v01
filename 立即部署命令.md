# 🚀 立即部署 Edge Function

## ✅ 已完成準備
- ✅ Supabase CLI 已安裝（版本 2.54.11）
- ✅ 檔案已準備好（619 行）

## 📋 現在執行這些命令

請在你的終端機中執行以下命令（按順序）：

### 1. 登入 Supabase

```bash
cd /Users/mingyou/Documents/cursor/NetSuite_Platform
supabase login
```

這會：
- 打開瀏覽器
- 讓你登入 Supabase 帳號
- 授權 CLI 存取

### 2. 連結專案

登入成功後，執行：

```bash
supabase link --project-ref mjjhopllbogcxqsofjjw
```

### 3. 部署 Edge Function

連結成功後，執行：

```bash
supabase functions deploy sync-netsuite
```

---

## 🎯 一條龍命令（複製貼上）

如果你想一次看到所有命令：

```bash
cd /Users/mingyou/Documents/cursor/NetSuite_Platform && \
supabase login && \
supabase link --project-ref mjjhopllbogcxqsofjjw && \
supabase functions deploy sync-netsuite
```

**注意**：`supabase login` 會打開瀏覽器，需要你手動登入。

---

## ✅ 部署成功後

你會看到類似這樣的訊息：

```
Deployed Function sync-netsuite
Version: 15
```

然後：
1. **在網站上觸發同步測試**
2. **查看 Dashboard 日誌**確認是否正常

---

## ⚠️ 如果遇到問題

**問題：`supabase login` 沒有打開瀏覽器**
- 解決：終端機會顯示一個 URL，手動複製到瀏覽器打開

**問題：連結時說找不到專案**
- 檢查 Project Ref 是否正確：`mjjhopllbogcxqsofjjw`
- 確認你已登入正確的 Supabase 帳號

**問題：部署失敗，說找不到檔案**
- 確認你在正確的目錄：`/Users/mingyou/Documents/cursor/NetSuite_Platform`
- 確認檔案存在：`supabase/functions/sync-netsuite/index.ts`


# 🔄 Edge Function 部署替代方案

## 問題
Dashboard 編輯器貼上後行數變少（620 行 → 400+ 行），可能是：
- Dashboard 編輯器的字元限制
- 某些特殊字元被過濾
- 複製貼上時的問題

## ✅ 解決方案

### 方案 1：使用 Supabase CLI（最可靠）

#### 安裝 CLI

```bash
# 方法 A：使用 Homebrew（推薦）
brew install supabase/tap/supabase

# 方法 B：使用 npm
npm install -g supabase
```

#### 部署步驟

```bash
# 1. 登入 Supabase
supabase login

# 2. 連結專案（Project Ref: mjjhopllbogcxqsofjjw）
cd /Users/mingyou/Documents/cursor/NetSuite_Platform
supabase link --project-ref mjjhopllbogcxqsofjjw

# 3. 部署
supabase functions deploy sync-netsuite
```

---

### 方案 2：分段複製到 Dashboard

如果必須用 Dashboard，可以分段複製：

1. **複製前 200 行** → 貼上並保存
2. **複製 201-400 行** → 貼上並保存
3. **複製 401-620 行** → 貼上並保存

**注意**：這個方法不推薦，因為可能會覆蓋之前的內容。

---

### 方案 3：檢查 Dashboard 設定

某些 Supabase Dashboard 版本可能有：
- **程式碼編輯器設定**：檢查是否有 "Minify" 或 "Format" 選項被啟用
- **字元限制**：查看是否有字元數限制提示
- **瀏覽器問題**：嘗試用不同瀏覽器或無痕模式

---

### 方案 4：直接使用檔案上傳（如果 Dashboard 支援）

某些 Supabase Dashboard 版本支援直接上傳檔案：
1. 在 Edge Function 頁面查找 "Upload" 或 "Import" 按鈕
2. 選擇 `supabase/functions/sync-netsuite/index.ts` 檔案上傳

---

## 🎯 推薦做法

**直接用 CLI 部署**是最可靠的方法：

```bash
# 安裝（如果還沒安裝）
brew install supabase/tap/supabase

# 登入（會打開瀏覽器）
supabase login

# 連結專案
cd /Users/mingyou/Documents/cursor/NetSuite_Platform
supabase link --project-ref mjjhopllbogcxqsofjjw

# 部署
supabase functions deploy sync-netsuite
```

這樣可以確保：
- ✅ 所有 619 行都被正確部署
- ✅ 沒有字元被過濾或截斷
- ✅ 自動處理編碼和格式問題

---

## ⚠️ 如果 Dashboard 複製後行數不對

請檢查：
1. **是否真的少了內容**：對比 Dashboard 中的最後幾行和本地檔案
2. **是否有錯誤訊息**：Dashboard 可能會顯示語法錯誤
3. **查看部署後的日誌**：看看是否有執行錯誤

如果內容確實被截斷，建議用 CLI 方式部署。

